// Basic types
let integer: number = 1;
let decimal: number = 1.1;
let text: string = 'abc';
let bool: boolean = true;

let method: (message: string) => void = message => alert(message);

enum DepartmentEnum {
    Development,
    Business
}
